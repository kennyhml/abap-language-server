/**
 * Adapters that integrate directly with the vscode framework.
 *
 * Usually, this means some sort of provider that is plugged directly into the
 * vscode context and thus implements some interface that imposes.
 */
export * from './filesystem';
export * from './decoration';
